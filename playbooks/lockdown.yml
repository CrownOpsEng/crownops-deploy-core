---
- name: Lock down public SSH after Tailscale validation
  hosts: core_hosts
  become: true
  roles:
    - role: crownops.deploy_base.network_lockdown
      tags: [lockdown, ssh, tailscale, firewall]
