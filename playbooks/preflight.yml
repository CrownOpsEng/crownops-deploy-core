---
- name: Preflight validation for deployment core
  hosts: localhost
  connection: local
  become: false
  gather_facts: false
  roles:
    - role: preflight_validate
      tags: [preflight, validate]
